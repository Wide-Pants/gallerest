const search_list = document.querySelectorAll("#search_li li");

search_list.forEach((list,i)=>{
        /*list.addEventListener("click",(e)=>{
            console.log(e.target.innerHTML)
            history.pushState({},`${e.target.innerHTML}`,`/${e.target.innerHTML}`)
        })*/
    }
)